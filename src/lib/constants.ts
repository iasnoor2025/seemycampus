/**
 * Shared constants that can be used in both client and server code
 * This file must not import any Node.js-only modules or database code
 */

export const baseUrl = process.env.NEXT_PUBLIC_BASE_URL || "https://seemycampus.com"
